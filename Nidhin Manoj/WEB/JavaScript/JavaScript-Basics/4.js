// if
let uname="Nidhin";
if(uname=="Nidhin");
{
    console.log("It's Nidhin");
}
// if-else

let k= 10;
if(k%2==0)
console.log(`${k} is even`);
else
console.log(`${k} is not even`);

// nested if
let a= 10;
let b= 30;
let c=20;

if(a>b){
    if(a>c) console.log(`${a} is max`);
    else console.log(`${c} is max`);
}
else
{
    if(b>c)console.log(`${b} is max`);
    else console.log(`${c} is max`);
}